{
  "name": "estimator",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/jest": "^23.3.0",
    "@types/react-dom": "^16.0.6",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "surge": "yarn build && echo 'estimate.express' > ./build/CNAME && surge ./build"
  }
}
